import express from "express";

/**
 * Placeholder for optional /anagine/guppy/graphql proxy route.
 * Current GraphQL proxy is exposed at /graphql via src/server/graphql.js.
 */
export default function createGuppyProxyRouter() {
  return express.Router();
}
